def gv

pipeline {
    agent any
    stages {
        stage("init") {
            steps {
               // script {
                 // def is = new File("data.csv").newInputStream()
                   // is.eachLine { 
                    //println it
                    //}
                    //is.close()
                    sh 'pwd'
                    sh 'ls -l'
               // }
            }
        }
    }
}